commands:
  parse:
    arguments:
      yaml:
        type: file
        required: true
        help: Path to yaml file to validate.
      schema:
        type: file
        help: Path to schema to validate yaml against.
    output:
      type: object

#!/usr/bin/env node

app = require('../lib/app')

if (process.argv[2] == 'parse') {
  res = app.parse(process.argv[3], process.argv[4])

  if (res.valid) {
    process.stdout.write(res.yaml)
    process.exit(0)

  } else {
    process.stderr.write(res.errorsText)
    process.exit(1)
  }

}
